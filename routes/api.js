const express = require('express');
const router = express.Router();

// GET /api - get all pieces of music in the concert
router.get('/api', (req, res) => {

})

// GET /api/:id - get all info on piece of music
router.get('/api/:id', (req, res) => {

})

// POST /api - add a piece to the concert
// POST /api/:id - add additional bits of information to a piece